stage('Run Task Based on Mode') {
            steps {
                script {
                    if (params.MODE.contains('Run Python Script')) {
                        echo "1" // Replace with actual script logic if needed
                    }
                    if (params.MODE.contains('Run Ansible Playbook')) {
                        echo "2" // Replace with ansible execution if needed
                    }
                    if (params.MODE.contains('Echo HelloWorld')) {
                        echo "3" // Echo HelloWorld as per the mode
                    }
                }
            }
        }
